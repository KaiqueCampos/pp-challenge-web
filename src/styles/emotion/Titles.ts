import styled from '@emotion/styled'

export const TitleSize14 = styled.h3({
    fontSize: '0.9rem',
    margin: 'initial',
    lineHeight: '1rem',
},
    props => ({ color: props.color }),
)

export const TitleSize32 = styled.h1({
    fontSize: '2.1rem',
    margin: 'initial',
    lineHeight: '1rem',
},
    props => ({ color: props.color }),
)

export const TitleSize16 = styled.h2({
    fontSize: '1.05rem',
    margin: 'initial',
    lineHeight: '1rem',
},
    props => ({ color: props.color }),
)